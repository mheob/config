---
'@mheob/oxlint-config': minor
'@mheob/oxfmt-config': minor
---

Add `@mheob/oxlint-config` and `@mheob/oxfmt-config` packages.

- `@mheob/oxlint-config` provides a shared OXLint configuration with presets for base, React, Storybook, and Tailwind CSS
- `@mheob/oxfmt-config` provides a shared OXFmt formatter configuration
