---
'@mheob/commitlint-config': patch
'@mheob/oxfmt-config': patch
'@mheob/oxlint-config': patch
'@mheob/tsconfig': patch
---

Switch the repository's package manager from Bun to pnpm. The published package contents are unchanged; only the install instructions in the READMEs now use `pnpm add -D`.
