---
'@mheob/oxlint-config': patch
---

Update the bundled JS plugins and raise their peer dependency floors: `eslint-plugin-jsonc` to `^3.4.1`, `eslint-plugin-storybook` to `^10.5.7`, and `eslint-plugin-yml` to `^3.8.1`.
